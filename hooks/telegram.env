TELEGRAM_BOT_TOKEN=""
# send message to bot with "hello" to get chat id, then use the following command to get chat id
# curl -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/getUpdates"
# .result[0].message.chat.id
TELEGRAM_CHAT_ID=""
